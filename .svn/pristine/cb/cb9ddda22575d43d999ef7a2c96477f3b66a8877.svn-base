import LocalizedStrings from 'react-native-localization';

import EN from './en';
import CH from './ch';

export default strings = new LocalizedStrings({
    en: {
        ...EN,
    },
    ch: {
        ...CH,
    },
});
